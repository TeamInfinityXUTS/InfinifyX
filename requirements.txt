# ============================================================
# InfinifyX — Python Dependencies
# ============================================================
# Install all:
#   pip install -r requirements.txt
#
# NOTE: PyTorch and torch-geometric must be installed separately
# with the correct CUDA/CPU variant (see instructions below).
# ============================================================


# ── Core ML / Deep Learning ─────────────────────────────────
# Target: PyTorch 2.6 + CUDA 12.6
# (Tesla T4 / driver 595 / CUDA 13.2 — driver is backward-compatible with cu126 wheels)
#
# Step 1 — install PyTorch (run this BEFORE pip install -r requirements.txt):
#   pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 \
#       --index-url https://download.pytorch.org/whl/cu126
#
# CPU-only fallback:
#   pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 \
#       --index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# ── PyTorch Geometric ────────────────────────────────────────
# Step 2 — install torch-geometric (after PyTorch is installed):
#   pip install torch-geometric
#   pip install torch-scatter torch-sparse torch-cluster torch-spline-conv \
#       -f https://data.pyg.org/whl/torch-2.6.0+cu126.html
torch-geometric

# ── Computer Vision ──────────────────────────────────────────
ultralytics              # YOLOv8 (Yolov8_best.pt, custom CBAM/SE backbone)
opencv-python            # cv2 — frame decode/encode in inference server

# ── Web / Inference Server ───────────────────────────────────
fastapi                  # REST + WebSocket API framework
uvicorn[standard]        # ASGI server (includes websockets + httptools)
python-multipart         # required by FastAPI for form/file handling

# ── MLOps / Experiment Tracking ─────────────────────────────
clearml                  # Pipeline orchestration, dataset versioning, artifact upload

# ── Classical ML ─────────────────────────────────────────────
scikit-learn             # LogisticRegression, StandardScaler (Risk_prediction.py)
joblib                   # Model serialisation for RiskPredictionModel (.pkl)

# ── Data / Utilities ─────────────────────────────────────────
numpy                    # Numerical arrays throughout the codebase
tqdm                     # Progress bars in feature engineering pipeline
pyyaml                   # YAML config parsing (yolo_trainer.py, dataset configs)
matplotlib               # Risk score distribution plots (Risk_pipeline.py)

# ── Augmentation (MLOps DataProcessing) ─────────────────────
albumentations           # Geometric + pixel-level augmentation pipeline
